diff --git a/ggml/src/ggml-cpu/quants.c b/ggml/src/ggml-cpu/quants.c
index 5e36459..82bce90 100644
--- a/ggml/src/ggml-cpu/quants.c
+++ b/ggml/src/ggml-cpu/quants.c
@@ -206,11 +206,11 @@ void ggml_vec_dot_q2_0_q8_0_generic(int n, float * GGML_RESTRICT s, size_t bs, c
 
             for (int b = 0; b < 8; ++b) {
                 const uint8_t byte = qs[b];
-                // Extract 4 two-bit values, map {0,1,2,3} -> {-1,0,1,2}
-                sumi_block += ((int)((byte >> 0) & 3) - 1) * qy[b*4 + 0];
-                sumi_block += ((int)((byte >> 2) & 3) - 1) * qy[b*4 + 1];
-                sumi_block += ((int)((byte >> 4) & 3) - 1) * qy[b*4 + 2];
-                sumi_block += ((int)((byte >> 6) & 3) - 1) * qy[b*4 + 3];
+                // Extract 4 two-bit values, map {0,1,2,3} -> {-3,-1,1,3}
+                sumi_block += (2 * (int)((byte >> 0) & 3) - 3) * qy[b*4 + 0];
+                sumi_block += (2 * (int)((byte >> 2) & 3) - 3) * qy[b*4 + 1];
+                sumi_block += (2 * (int)((byte >> 4) & 3) - 3) * qy[b*4 + 2];
+                sumi_block += (2 * (int)((byte >> 6) & 3) - 3) * qy[b*4 + 3];
             }
 
             sumi += d1 * sumi_block;
diff --git a/ggml/src/ggml-cuda/vecdotq.cuh b/ggml/src/ggml-cuda/vecdotq.cuh
index f2a6f20..58a79eb 100644
--- a/ggml/src/ggml-cuda/vecdotq.cuh
+++ b/ggml/src/ggml-cuda/vecdotq.cuh
@@ -768,7 +768,8 @@ static __device__ __forceinline__ float vec_dot_q2_0_q8_1(
 
     // Apply Q2_0's single scale and this chunk's Q8_1 scale
     const float d8 = __low2float(bq8_1_chunk->ds);
-    return d2 * d8 * sumi;
+    const float sum8 = __high2float(bq8_1_chunk->ds);
+    return d2 * (2.0f * d8 * sumi - 3.0f * sum8);
 }
 
 static __device__ __forceinline__ float vec_dot_q4_0_q8_1(
diff --git a/ggml/src/ggml-quants.c b/ggml/src/ggml-quants.c
index 1ebc50a..27005d7 100644
--- a/ggml/src/ggml-quants.c
+++ b/ggml/src/ggml-quants.c
@@ -79,13 +79,27 @@ void quantize_row_q2_0_ref(const float * GGML_RESTRICT x, block_q2_0 * GGML_REST
     const int nb = k / qk;
 
     for (int i = 0; i < nb; i++) {
-        // Compute scale as max absolute value in the block
+        // Symmetric four-level codebook {-3, -1, +1, +3}.
         float amax = 0.0f;
         for (int j = 0; j < qk; j++) {
             const float a = fabsf(x[i*qk + j]);
             if (a > amax) amax = a;
         }
-        const float d = amax;
+        float d = amax / 3.0f;
+        for (int iteration = 0; iteration < 4 && d > 0.0f; ++iteration) {
+            const float id = 1.0f / d;
+            float sum_xq = 0.0f;
+            float sum_q2 = 0.0f;
+            for (int j = 0; j < qk; ++j) {
+                int q = (int)roundf((x[i*qk + j] * id + 3.0f) * 0.5f);
+                if (q < 0) q = 0;
+                if (q > 3) q = 3;
+                const int level = 2*q - 3;
+                sum_xq += x[i*qk + j] * level;
+                sum_q2 += level * level;
+            }
+            d = sum_xq / sum_q2;
+        }
         const float id = d > 0.0f ? 1.0f / d : 0.0f;
 
         y[i].d = GGML_FP32_TO_FP16(d);
@@ -95,11 +109,10 @@ void quantize_row_q2_0_ref(const float * GGML_RESTRICT x, block_q2_0 * GGML_REST
             y[i].qs[j] = 0;
         }
 
-        // Encode 2-bit values: round(w/d) clamped to [-1, 2], then add 1
-        // 00 (-1) = -scale, 01 (0) = 0, 10 (+1) = +scale, 11 (+2) = 2*scale
+        // Quantize to the nearest odd integer in {-3, -1, +1, +3}.
         for (int j = 0; j < qk; ++j) {
             const float w = x[i*qk + j];
-            int q = (int)roundf(w * id) + 1;
+            int q = (int)roundf((w * id + 3.0f) * 0.5f);
             if (q < 0) q = 0;
             if (q > 3) q = 3;
             const int byte_index = j / 4;
@@ -450,8 +463,7 @@ void dequantize_row_q2_0(const block_q2_0 * GGML_RESTRICT x, float * GGML_RESTRI
             const int byte_index = j / 4;
             const int bit_offset = (j % 4) * 2;
             const uint8_t q = (x[i].qs[byte_index] >> bit_offset) & 0x03;
-            // 00=-1, 01=0, 10=+1, 11=+2
-            y[i*qk + j] = ((int)q - 1) * d;
+            y[i*qk + j] = (2 * (int)q - 3) * d;
         }
     }
 }
diff --git a/src/llama-quant.cpp b/src/llama-quant.cpp
index 34ff25d..92dd082 100644
--- a/src/llama-quant.cpp
+++ b/src/llama-quant.cpp
@@ -286,7 +286,18 @@ static void llama_tensor_dequantize_impl(
 
 static bool tensor_allows_quantization(const llama_model_quantize_params * params, llm_arch arch, const ggml_tensor * tensor) {
     // trivial checks first -- no string ops needed
-    if (params->only_copy)       return false;
+    if (params->only_copy) {
+        if (params->tt_overrides == nullptr) {
+            return false;
+        }
+        const std::string name = ggml_get_name(tensor);
+        for (const llama_model_tensor_override * p = params->tt_overrides; p->pattern != nullptr; ++p) {
+            if (std::regex_search(name, std::regex(p->pattern))) {
+                return true;
+            }
+        }
+        return false;
+    }
 
     // quantize only 2D and 3D tensors (experts)
     if (ggml_n_dims(tensor) < 2) return false;
@@ -705,10 +716,10 @@ static ggml_type llama_tensor_get_type(quantize_state_impl & qs, const llama_mod
         return params->output_tensor_type;
     }
 
-    ggml_type new_type = default_type;
+    ggml_type new_type = params->only_copy ? tensor->type : default_type;
 
     // get more optimal quantization type based on the tensor shape, layer, etc.
-    if (ggml_is_quantized(default_type)) {
+    if (params->only_copy || ggml_is_quantized(default_type)) {
         // if the user provided tensor types - use those
         bool manual = false;
         if (!qs.tensor_type_patterns.empty()) {
@@ -727,7 +738,7 @@ static ggml_type llama_tensor_get_type(quantize_state_impl & qs, const llama_mod
         }
 
         // if not manual - use the standard logic for choosing the quantization type based on the selected mixture
-        if (!manual && !params->pure) {
+        if (!params->only_copy && !manual && !params->pure) {
             new_type = llama_tensor_get_type_impl(qs, new_type, tensor, params->ftype, tm.category);
         }
 
